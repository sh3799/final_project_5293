---
title: "code"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(tidyverse)
# test
res <- read_csv("restaurant_data.csv") %>% na.omit(res)

sb <- res %>% filter(res$DBA == "STARBUCKS") %>% group_by(CAMIS)
shops_sb <- summarize(sb, shops = n())

zip_unique <- res[, 1:8] %>% distinct()
zip_shops <-
  zip %>% group_by(ZIPCODE) %>% summarise(shops = n_distinct(CAMIS))
```

